# GENERATED PAC — see ../README.md and scripts/gen-pac.sh. Only this file is hand-maintained.
[package]
name = "msp430fr2476"
version = "0.1.0"
edition = "2021"
description = "Peripheral access API for MSP430FR2476 (generated with svd2rust)"
license = "MIT OR Apache-2.0"
repository = "https://github.com/experientials/msp-fw"
categories = ["embedded", "hardware-support", "no-std"]
keywords = ["msp430", "pac", "svd2rust"]

[dependencies]
vcell = "0.1.3"
critical-section = { version = "1.1", optional = true }
msp430 = { version = "0.4.0", optional = true }
msp430-rt = { version = "0.4.0", optional = true }

[features]
default = []
rt = ["dep:msp430-rt"]
critical-section = ["dep:critical-section", "dep:msp430"]

[package.metadata.docs.rs]
features = ["rt", "critical-section"]
